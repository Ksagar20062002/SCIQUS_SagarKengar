<!-- Navbar.ejs -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Management System</title>
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
        }

        header {
            background-color: #3498db;
            padding: 15px;
            text-align: center;
        }

        nav {
            background-color: #2c3e50;
            overflow: hidden;
        }

        nav a {
            float: left;
            display: block;
            color: white;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
        }

        nav a:hover {
            background-color: #ddd;
            color: black;
        }

        .logout-form {
            float: right;
            padding: 14px 16px;
        }

        .logout-button {
            background-color: #e74c3c;
            color: white;
            border: none;
            cursor: pointer;
        }

        .logout-button:hover {
            background-color: #c0392b;
        }
    </style>
</head>

<body>
    <header>
        <h1>Student Management System</h1>
    </header>
    <nav>
        <form class="logout-form" action="/Sciqus" method="GET">
            <button class="logout-button" type="submit">Logout</button>
        </form>
    </nav>
</body>

</html>
